Service Manual is the "configs/thinkpad-styled" entry (configs/thinkpad-styled) from the community awesome-fastfetch-collection (https://github.com/Chintanpatel24/awesome-fastfetch-collection), MIT licensed. Ported for Ryoku: Replaced the ThinkPad ASCII logo (file: ~/.config/fastfetch/thinkpad.txt) with no logo; the ruled table is self-contained. Dropped the right vertical wall: upstream drew it with cursor-jump escapes (\e[83C/\e[83D) that do not survive piping or width measurement. Kept the left ║ wall, full-width red rules and the aligned ╤/╪/╧ column so the ruled-table identity holds. Narrowed the rules from 83 to 64 columns so the whole table fits under 100 columns with no wrapping. Routed the dominant red through display.color.keys ({#keys}) for all rules, the left wall and the column bar; per-icon ANSI colours ({#31}..{#36}) and the title host-blue / palette squares stay as terminal-following palette refs. Converted the raw \e[..m escapes to fastfetch {#..} palette refs and dropped the already-disabled netio module. It ships as a single config file using only built-in Fastfetch modules, with no external runtime assets.
