Pastel Brief is the "configs/win-11-config" entry (configs/win-11-config) from the community awesome-fastfetch-collection (https://github.com/Chintanpatel24/awesome-fastfetch-collection), MIT licensed. Ported for Ryoku: Replaced the Windows braille logo (file: C:/Users/.../ascii.txt) with the built-in small Arch logo. Moved the Catppuccin palette into display.color slots (keys=sky, title=pink) plus inline truecolor for the peach/green/teal keys so the accent recolours. Rebuilt the title as a format string with colour slots instead of the fixed title.color block. Dropped the logo colour block (1-9) that only tinted the removed Windows art. It ships as a single config file using only built-in Fastfetch modules, with no external runtime assets.
