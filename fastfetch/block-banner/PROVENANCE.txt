Block Banner is the "kuruma-config" entry (configs/kuruma-config/host.jsonc) from the community awesome-fastfetch-collection (https://github.com/Chintanpatel24/awesome-fastfetch-collection), MIT licensed, originally by kuruma-config. Ported for Ryoku: Banner text changed from the author hostname KURUMA to ARCH, redrawn in the same ANSI-Shadow block style with all six rows 32 columns wide. Converted upstream's icon-only tree (no word labels, no right border, stepped left column) into a uniform closed label card: every row is now box glyph + icon + padded word label + right border, with rounded top/bottom rules; added per-module icons and labels. Dominant green accent {#32} mapped to the {#keys} slot (display.color.keys=#a6e3a1); blue {#34} kept as a palette reference for the DE/WM group. Added display.color.output=#c5c9c5 so value text follows the output slot. display.separator changed from three spaces to one space. Dropped the two 62-column constant rules and the constants array, the trailing break, and the leftover outputColor. Normalized upstream logo source:none to type:none; added $schema and the ryoku header comment. It ships as a single config file using only built-in Fastfetch modules, with no external runtime assets.
