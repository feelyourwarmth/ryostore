Soft Column is the "aayushchalise" entry (configs/aayushchalise/config.jsonc) from the community awesome-fastfetch-collection (https://github.com/Chintanpatel24/awesome-fastfetch-collection), MIT licensed, originally by aayushchalise. Ported for Ryoku: Logo changed from kitty PNG (~/.config/fastfetch/images/arch.png) to builtin Arch logo; added padding.right 2. Removed the OS-age `command` module (ran `stat -c %W /`); no shell-out allowed. packages format {} resolved to the empty am-system field (0 on Arch); changed to {pacman}. Upstream memory key icon U+FB19 is a Hebrew presentation form, not a memory glyph; replaced with U+F035B (nf-md-memory). Removed per-module keyColor 34; accent now flows through display.color.keys #c6a0f6. It ships as a single config file using only built-in Fastfetch modules, with no external runtime assets. The preset now ships that design's own emblem (arch.png): The Arch mark the upstream config shipped in its images folder. It installs beside the config and is referenced from it; the artwork is redistributed as it appears in the upstream MIT-licensed collection and remains the property of its creators. Swap it in Settings -> Fastfetch, or delete the file to fall back to the built-in Arch logo.
